<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.newsapp.newsmind.mapper.UserSourceMapper">

    <insert id="insertUserSource">
        INSERT INTO user_source (user_id, domain, source_name)
        VALUES (#{userId}, #{domain}, #{sourceName})
    </insert>

    <delete id="deleteUserSource">
        DELETE FROM user_source
        WHERE user_id = #{userId}
          AND domain = #{domain}
    </delete>

    <select id="selectUserSourceList" resultType="map">
        SELECT domain,
            source_name AS sourceName,
            created_at  AS createdAt
        FROM user_source
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
